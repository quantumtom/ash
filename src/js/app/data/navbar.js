define('data/navbar', function () {
  /**
   * A module represnting a navbar dataset.
   * @exports data/navbar
   * @type {{title: string}}
   */
  var navbarData = {
    title: 'Navbar'
  };

  return navbarData;
});
