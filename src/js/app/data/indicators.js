define('data/indicators',function () {
  /**
   * A module representing an indicators dataset.
   * @exports data/indicators
   * @type {{title: string}}
   */
  var indicatorsData = {
    title: 'Indicators'
  };

  return indicatorsData;
});
