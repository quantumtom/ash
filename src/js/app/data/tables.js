define('data/tables', function () {
  /**
   * A module representing a tables dataset.
   * @exports data/tables
   * @type {{title: string}}
   */
  var tablesData = {
    title: 'Tables'
  };

  return tablesData;
});
