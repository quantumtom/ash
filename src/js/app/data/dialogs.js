define('data/dialogs', function () {
  /**
   * A module representing a dialogs dataset.
   * @exports data/dialogs
   * @type {{title: string}}
   */
  var dialogsData = {
    title: 'Dialogs'
  };

  return dialogsData;
});
