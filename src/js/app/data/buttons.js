define('data/buttons', function () {
  /**
   * A module representing a buttons dataset.
   * @exports data/buttons
   * @type {{title: string}}
   */
  var buttonsData = {
    title: 'Buttons'
  };

  return buttonsData;
});
