define('data/navs', function () {
  /**
   * A module representing a navs dataset.
   * @exports data/navs
   * @type {{title: string}}
   */
  var navsData = {
    title: 'Navs'
  };

  return navsData;
});
