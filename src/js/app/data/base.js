define('data/base', function () {
  /**
   * A module representing a base dataset.
   * @exports data/base
   * @type {{title: string, body: string}}
   */
  var baseData = {
    title: 'Base Class',
    body: 'The base class is an abstract template for creating new modules.'
  };

  return baseData;
});
