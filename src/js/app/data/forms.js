define('data/forms', function () {
  /**
   * A module representing a forms dataset.
   * @exports data/forms
   * @type {{title: string}}
   */
  var formsData = {
    title: 'Forms'
  };

  return formsData;
});
